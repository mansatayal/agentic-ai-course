[project]
name = "07-react"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mansatayal", email = "mansa.aggarwal.172005@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "groq>=1.7.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
07-react = "07_react:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
