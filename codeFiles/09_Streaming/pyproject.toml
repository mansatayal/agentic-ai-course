[project]
name = "09-streaming"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mansatayal", email = "mansa.aggarwal.172005@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "groq>=1.7.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
09-streaming = "09_streaming:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
